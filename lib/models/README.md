# models/

Plain Dart data classes mirroring the DDD Firestore collections exactly:
`employee.dart`, `employee_hour.dart`, `client.dart`, `client_payment.dart`,
`settings.dart`.

Populated in: **Data Layer phase**.
