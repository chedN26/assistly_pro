# repositories/mock/

`MockAuthRepository`, `MockEmployeeRepository`, `MockClientRepository`,
`MockSettingsRepository` — in-memory implementations used for all
development phases prior to Firebase integration.

Populated in: **Authentication phase** (mock auth) and **Data Layer phase**
(remaining mock repositories).
