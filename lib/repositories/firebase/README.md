# repositories/firebase/

`FirebaseAuthRepository`, `FirebaseEmployeeRepository`,
`FirebaseClientRepository`, `FirebaseSettingsRepository` — Cloud Firestore
implementations that swap in for the mock repositories with no changes
required to Providers or UI.

Populated in: **Firebase phase**.
